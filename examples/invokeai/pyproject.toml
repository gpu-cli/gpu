[project]
name = "invokeai-example"
version = "0.1.0"
description = "InvokeAI - Professional Stable Diffusion web UI"
requires-python = ">=3.11"
dependencies = [
    "invokeai[xformers]",
]

[tool.setuptools]
py-modules = []
