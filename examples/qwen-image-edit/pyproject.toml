[project]
name = "qwen-image-edit-template"
version = "0.1.0"
description = "GPU CLI template for ComfyUI with Qwen Image Edit workflow"
requires-python = ">=3.10"
dependencies = [
    "huggingface-hub[cli]",
    "tqdm",
    "requests",
]

[tool.setuptools]
py-modules = []
