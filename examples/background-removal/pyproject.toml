[project]
name = "background-removal-template"
version = "0.1.0"
description = "GPU CLI template for InvokeAI with BiRefNet background removal"
requires-python = ">=3.11"
dependencies = [
    "invokeai[xformers]",
    "timm",
    "kornia",
]

[tool.setuptools]
py-modules = []
