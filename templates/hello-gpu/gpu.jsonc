{
  "$schema": "https://gpu-cli.sh/schema/v1/gpu.json",
  "project_id": "hello-gpu",
  "provider": "runpod",
  "template": {
    "name": "Hello GPU",
    "description": "A minimal test template to verify GPU CLI works",
    "author": "gpu-cli"
  },
  "gpu_types": [{ "type": "NVIDIA GeForce RTX 4090" }],
  "min_vram": 8,
  "outputs": ["outputs/"],
  "startup": "python hello.py"
}
